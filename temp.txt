import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion";
import { Button } from "@/components/ui/button";
import { Shield, Code, Clock, MessageCircle, BookOpen, RefreshCw } from "lucide-react";

const faqs = [
  {
    icon: Shield,
    question: "É realmente 100% gratuito?",
    answer: "Sim. Sem pegadinhas, sem cartão de crédito. Em parceria com Fatec e USP.",
  },
  {
    icon: Code,
    question: "Preciso saber programar?",
    answer: "Não. A trilha C# Iniciante parte do zero. Basta dedicação de 4-6h/semana.",
  },
  {
    icon: Clock,
    question: "Quanto tempo dura?",
    answer: "4-6 semanas por trilha. Você avança no seu ritmo, mas com prazos sugeridos.",
  },
  {
    icon: MessageCircle,
    question: "Como funciona o suporte?",
    answer: "Discord ativo 24/7, code reviews semanais, mentorias em grupo quinzenais.",
  },
  {
    icon: BookOpen,
    question: "Posso fazer mais de uma trilha?",
    answer: "Sim! Recomendamos C# + IA para stack completa.",
  },
  {
    icon: RefreshCw,
    question: "Como funciona a mentoria reversa?",
    answer: "Alunos avançados ajudam iniciantes. XP + networking real.",
  },
];

export const FAQ = () => {
  const leftColumn = faqs.slice(0, 3);
  const rightColumn = faqs.slice(3);

  return (
    <section id="faq" className="py-20 px-4 bg-gradient-to-b from-background to-bg-elevated">
      <div className="container mx-auto max-w-6xl">
        <div className="mb-12 text-center">
          <h2 className="mb-4 text-4xl font-bold text-white md:text-5xl">Dúvidas Frequentes</h2>
          <p className="text-lg text-muted-foreground">Tudo o que você precisa saber antes de começar</p>
        </div>

        <div className="grid gap-6 md:grid-cols-2">
          {[leftColumn, rightColumn].map((items, columnIndex) => (
            <Accordion key={columnIndex} type="single" collapsible className="space-y-4">
              {items.map((faq, index) => {
                const Icon = faq.icon;
                return (
                  <AccordionItem
                    key={faq.question}
                    value={`item-${columnIndex}-${index}`}
                    className="rounded-lg border border-border px-6 transition-colors hover:border-primary/50"
                  >
                    <AccordionTrigger className="text-left hover:text-primary hover:no-underline">
                      <div className="flex items-center gap-3">
                        <div className="flex h-8 w-8 flex-shrink-0 items-center justify-center rounded-lg bg-primary/10">
                          <Icon className="h-4 w-4 text-primary" />
                        </div>
                        <span>{faq.question}</span>
                      </div>
                    </AccordionTrigger>
                    <AccordionContent className="ml-11 text-muted-foreground">{faq.answer}</AccordionContent>
                  </AccordionItem>
                );
              })}
            </Accordion>
          ))}
        </div>

        <div className="mt-12 rounded-2xl border border-border bg-bg-soft p-8 text-center">
          <p className="mb-4 text-lg text-white">Ainda tem dúvida?</p>
          <p className="mb-6 text-muted-foreground">Fale diretamente com nossa comunidade no Discord</p>
          <Button variant="outline" size="lg" asChild>
            <a href="https://discord.gg/4ucode" target="_blank" rel="noopener noreferrer">
              Entrar no Discord
            </a>
          </Button>
        </div>
      </div>
    </section>
  );
};
